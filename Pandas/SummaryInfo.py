#Indexes are labels for rows that permit fast lookup and relationship operators in pandas 

import pandas as pd 
type(Dataframe) 

#tells you how many (rows, columns) 
dataframe.shape

#tells you the names of columns 
dataframe.columns 

#allows you to see the first five rows 
database.head()

#allows you to see the last five rows 
database.tail()

#returns index, columns labels, datatype and other information 
info()
